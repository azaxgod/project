import 'package:flutter/material.dart';

class AnalyticsContractsPage extends StatelessWidget {
  const AnalyticsContractsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Аналитика контрактов')),
      body: const Center(child: Text('Аналитика контрактов (в разработке)')),
    );
  }
}

