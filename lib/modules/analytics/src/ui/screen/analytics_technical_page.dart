import 'package:flutter/material.dart';

class AnalyticsTechnicalPage extends StatelessWidget {
  const AnalyticsTechnicalPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Техническая аналитика')),
      body: const Center(child: Text('Техническая аналитика (в разработке)')),
    );
  }
}

