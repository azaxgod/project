import 'package:flutter/material.dart';

class AnalyticsVehiclesPage extends StatelessWidget {
  const AnalyticsVehiclesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Аналитика транспорта')),
      body: const Center(child: Text('Аналитика транспорта (в разработке)')),
    );
  }
}

